import { MinCountDirective } from './min-count.directive';

describe('MinCountDirective', () => {
  it('should create an instance', () => {
    const directive = new MinCountDirective();
    expect(directive).toBeTruthy();
  });
});
